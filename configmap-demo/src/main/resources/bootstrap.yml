spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
        strategy: restart_context
      config:
        enabled: true
        sources:
          - namespace: default
            name: hot-config